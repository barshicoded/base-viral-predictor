{
  "name": "viral-predictor",
  "version": "1.0.0",
  "description": "Prediction game for Base Social metrics",
  "main": "index.html",
  "scripts": {
    "dev": "npx serve .",
    "build:ios": "npx cap add ios && npx cap open ios",
    "build:android": "npx cap add android && npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
